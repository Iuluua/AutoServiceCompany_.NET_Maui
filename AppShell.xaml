<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MelisaIuliaProiect.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MelisaIuliaProiect"
    Shell.FlyoutBehavior="Disabled"
    Title="MelisaIuliaProiect">


    <TabBar>
        <Tab Title="Home">
            <ShellContent ContentTemplate="{DataTemplate local:MainPage}" Route="MainPage" />
        </Tab>
        <Tab Title="About">
            <ShellContent ContentTemplate="{DataTemplate local:AboutPage}" Route="AboutPage" />
        </Tab>
        <Tab Title="Departments">
            <ShellContent ContentTemplate="{DataTemplate local:DepartmentEntryPage}" Route="DepartmentEntryPage" />
        </Tab>
        <Tab Title="Cities">
            <ShellContent ContentTemplate="{DataTemplate local:CityEntryPage}" Route="CityEntryPage" />
        </Tab>
        <Tab Title="Clients">
            <ShellContent ContentTemplate="{DataTemplate local:ClientEntryPage}" Route="ClientEntryPage" />
        </Tab>
    </TabBar>

</Shell>
